{% extends 'base.html.twig' %}
{% block card_open %}{% endblock %}
{% block card_close %}{% endblock %}
{% block title %}Modifier le locataire{% endblock %}
{% block body %}
    <div class="dashboard-header">
        <div><h1>Modifier le locataire</h1></div>
        <div class="page-actions">
            <a href="{{ path('app_tenant_index') }}" class="btn-sm">← Retour</a>
            {{ include('tenant/_delete_form.html.twig') }}
        </div>
    </div>
    <div class="card">
    {{ include('tenant/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    </div>
{% endblock %}