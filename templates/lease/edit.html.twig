{% extends 'base.html.twig' %}
{% block card_open %}{% endblock %}
{% block card_close %}{% endblock %}
{% block title %}Modifier le bail{% endblock %}
{% block body %}
    <div class="dashboard-header">
        <div><h1>Modifier le bail</h1></div>
        <div class="page-actions">
            <a href="{{ path('app_lease_index') }}" class="btn-sm">← Retour</a>
            {{ include('lease/_delete_form.html.twig') }}
        </div>
    </div>
    <div class="card">
    {{ include('lease/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    </div>
{% endblock %}